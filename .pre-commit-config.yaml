repos:
  - repo: local
    hooks:
      - id: rusty-hook
        name: Rusty Hook ShellCheck
        entry: rusty-hook
        language: system
        args: ["run", "shellcheck"] # Run the 'shellcheck' hook defined in .rusty-hook.toml
